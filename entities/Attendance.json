{
  "name": "Attendance",
  "type": "object",
  "properties": {
    "student_email": {
      "type": "string"
    },
    "student_name": {
      "type": "string"
    },
    "course_id": {
      "type": "string"
    },
    "course_name": {
      "type": "string"
    },
    "date": {
      "type": "string",
      "format": "date"
    },
    "status": {
      "type": "string",
      "enum": [
        "present",
        "absent",
        "late",
        "excused"
      ],
      "default": "present"
    },
    "notes": {
      "type": "string"
    },
    "marked_by": {
      "type": "string",
      "description": "Teacher email"
    }
  },
  "required": [
    "student_email",
    "course_id",
    "date",
    "status"
  ]
}